---
name: bela-hand-comic
description: 把主题或脚本做成手绘连环画、概念故事图或图文卡片。默认彩铅淡水彩与奶油纸张；人物、画风、配色、字体、画幅和排版都能按用户要求自定义。适用于手绘故事、AI 大白话解释图、连续角色插画、单图修改与多图拼版；阿朱、阿兔、贝拉是可选示例角色。
---

# Bela Hand Comic

先把事情讲明白，再把它画成容易看懂的小故事。默认采用手绘彩铅、淡水彩和暖奶油纸张；用户可以换成自己的角色，也可以重新指定配色、笔触、文字位置和版式。

## 先确定这次怎么画

1. 读取 [references/style-guide.md](references/style-guide.md)。其中的阿朱、阿兔、贝拉和配色是默认示例，用户自己的设定优先。
2. 询问或从当前材料确定：主题、读者、用途、角色、风格、画幅、张数或内容范围、画面原文、保存位置。能合理推断的项目直接说明默认值，不重复追问。
3. 使用用户提供的角色图或文字设定。可以换人物、物种、服装、发型和配饰，也可以不用人物。未指定时单幅默认 4:3，多图总览默认 3:4，画风默认手绘；未指定角色且没有参考时，可以用随包的阿朱、阿兔。
4. 把本次选定的角色与视觉规则写进当前主题的现行文档。规则只用于当前系列，不把本次修改升级为以后所有项目的偏好。
5. 使用当前助手可用的图片生成与编辑能力。有 `imagegen` Skill 时遵循它；其他环境遵循对应工具说明。只有文字能力时先交付故事、分镜和提示词，说明尚未生成图片。
6. 本 Skill 不提供图片模型、API 密钥、账户或生成额度。参考图数量和格式以实际图片工具的限制为准。

## 人物可换，同一组图要接得上

- 角色一致性指本次确认后保持一致，不代表必须使用作者的人物。
- 用户要自己的角色时，先记录外形、服装、标志物、配饰位置和允许变化的部分；有角色图时先查看再生图。需要角色设定图时，先做一张确认后用于后续页面。
- 表情、动作、朝向、景别和位置随每页内容变化，不反复贴同一个站姿。
- 使用阿朱、阿兔、贝拉时，遵循风格指南里的对应示例设定；贝拉是作者个人 IP，只有需要时才选用，不默认把她当作其他使用者的本人替身。
- 用户中途要换装或改角色时，先说明哪些页面需要一起调整；仅编辑用户指定或已授权的页面。
- 用户没有另作指定时，保留手绘笔触和纸张质感。指定新画风时，更新本次规则并检查角色、场景和字体是否协调。

## 生图前先检查已有图片

在用户指定的项目范围检查当前主题、同系列素材和脚本引用，列出 `本页表达 → 可复用图片 → 真正缺口`。同一动作或概念已被准确表达时直接复用；改标题或页码不构成重画理由。旧图会导致概念错误、缺少关键状态或用户明确要求修改时才编辑或新增。不要扫描无关目录。

## 先检查逻辑，再拆画面

1. 分清事实与比喻。角色、标签和箭头不能让读者得出相反结论。信息不确定时查证或标明待确认，不直接画成结论。
2. 面向零基础的概念绘本先写总流程：来源、参与者、输入、转换、输出、循环和停止条件；按题目需要区分准备、处理与使用阶段。
3. 沿流程检查“为什么需要、从哪里来、谁在做、怎么变成下一步、怎么结束”。列出最可能的错误复述并补齐逻辑断点。
4. 逻辑通过后拆页，每页写 `本页问题 / 口播落点 / 前后因果连接`。页数服从内容，不预设五张或七张。
5. 整套概念绘本的逐页内容确认后，先做一张最容易误解的关键 Demo；确认画风、文字和逻辑后再批量。用户已经明确批准的设定不用重复确认。局部修改只检查受影响部分。
6. 解释 Agent、Model、Skill 时可以说：`Agent 接任务，Model 负责想，Agent 调用 Skill 去做。` 这是帮助理解的简化说法，不代表所有 Agent 都用同一架构；不要画成 Model 独自执行外部动作。

## 选择参考图

| 本次需要 | 随包示例 |
|---|---|
| 小猪阿朱 | `assets/azhu-character-sheet.png` |
| 兔子阿兔 | `assets/atu-character-sheet.png` |
| 贝拉旁白 | `assets/bela-character-reference.png` |
| 黑色手写文字 | `assets/handwriting-style.png` |
| 日常故事构图 | `assets/style-story-scene.png` |
| 能力被调用的表现 | `assets/style-skill-activation.png` |
| 简洁概念关系图 | `assets/style-concept-map.png` |

生成前实际查看选中的图片。用户自己的角色图替换示例角色图；需要保持前后连续时加入上一幅成片。超出工具参考数量限制时优先当前编辑目标、角色、最相关风格图，不宣称不同工具有相同上限。

## 逐张生成与修改

1. 一幅只讲一个动作或关系。需要画面文字时先给出准确原文，默认一句短句；用户要更长内容时用分区或拆页保持可读。
2. 每次提示词重复本次角色设定、风格、文字原文、构图与连续性要求，不假设图片模型记得上一幅。
3. 每幅单独生成。用户要分格漫画时可先分别完成各格再拼版，避免一次生成整套导致中文和人物走样。
4. 修改时把现有成片作为编辑目标，写清仅修改什么、保留什么；不要因为改一处文字就重新设计全页。
5. 生成后逐字检查中文，查看人物、手脚、配饰、物体与箭头。视觉和逻辑都正确才算通过。
6. 真实操作截图保持原样。手绘故事或概念比喻不能冒充软件截图或实测证据。

## 文件与拼版

- 每个主题保留一份现行说明，图片放在 `图片/`；默认命名 `01-任务出现.png`，两位连续编号加 4–6 字内容概括。用户自己的命名规则优先。
- 同主题、同编号、同逻辑内容的修订沿用原名；新版验收通过且已有覆盖授权后替换旧版。没有覆盖或删除授权时先保留旧文件并说明，不扩展清理范围。
- 用户可指定单图、分格、横向流程、竖版总览和文字位置。画幅及最终像素按用途确定。
- 随包 `scripts/compose-seven-3x4.sh` 仅提供一种可选排版：7 张原图 → 1536×2048，前六张两列三行，第七张底部居中。需 Bash 和 FFmpeg；已有输出会停止，避免误覆盖。
- 其他张数、画幅和网格需要助手按本次要求另做拼版方案，不能宣称这个固定脚本自动支持所有布局。
- 拼版用原图缩放、留边与排列，不让图片模型重绘已经确认的成片。

## 完成前检查

检查当前设定是否被尊重、文字是否准确、手机上是否可读、各页是否连续、比喻是否引起误解；拼版还要检查原图是否完整、顺序是否正确。给出文件位置、尺寸和仍待确认的内容。区分 `分镜已准备 / Demo 待确认 / 本地图片已生成 / 已发布`，生成完成不等于发布完成。
