# 当前验证范围

核验日期：2026-09-21。本次发布使用作者已有的 7 张参考图，没有为了上传重新生成图片。

| 检查项 | 状态 |
|---|---|
| 公开版文件、角色图与相对链接 | 已通过 |
| Skill 格式 | 已通过 |
| 七图拼版与已有输出保护 | 已通过：1536×2048 输出；重复输出返回错误且原文件未变 |
| 从公开仓库安装并核对文件 | 已通过：六种 Agent 目标的项目级文件安装，实际目录中核心文件一致 |
| GitHub README 图片显示与截图 | 已通过：默认效果、安装、自定义与环境三处实际截图 |
| 本次从零生成新图 | 未测试，展示图是已有参考素材 |
| Windows / Linux 拼版 | 未测试 |
| 六种 Agent 的逐个客户端触发与完整生图 | 未测试；安装器支持不等于出图能力可用 |
| 任意自定义角色、画风、排版的生成质量 | 由实际模型和任务决定，需逐张验收 |

测试通过只说明对应步骤可用，不保证中文、角色或概念关系无需人工检查。

## 本次实际检查

环境：macOS、Node.js 24.14.1、Skills CLI 1.7.0、FFmpeg 8.1.1。Skill 格式检查通过，文档相对链接存在，7 张原图均已打开检查。

在独立临时项目中，从公开仓库执行以下命令，未覆盖作者本机已安装的 Skill：

```sh
npx skills add belalee-ai/bela-hand-comic --skill bela-hand-comic --agent codex --copy --yes
```

安装器识别到 1 个 Skill。对 SKILL.md、风格指南、客户端展示配置、拼版脚本及 7 张图片，共 11 个核心文件逐一比较 SHA-256，均一致；安装后的 Skill 格式检查通过。这轮仅验证了 Codex 项目级安装。六种目标的后续安装检查见下一节；全局安装位置未实测。

拼版使用随包 7 张不同尺寸的参考素材作输入，仅用于检查脚本，不把它当成一套连续故事。输出为 1536×2048，已目视检查排列、留边与原图完整性；再次写入同一路径返回错误，原文件哈希未变。

README 在浏览器实际打开，参考图正常显示；截图记录的是提交 b598352 的文档内容。没有把截图标成图片生成过程，也没有执行跨客户端新图生成测试。

## Agent 安装补充检查

2026-09-21，针对公开仓库提交 `d685928`，在另一个独立临时项目中执行：

```sh
npx skills add belalee-ai/bela-hand-comic --skill bela-hand-comic \
  --agent claude-code codex cursor opencode gemini-cli github-copilot \
  --copy --yes
```

安装器接受全部六个目标并正常完成。实际产生 `.claude/skills/bela-hand-comic` 和 `.agents/skills/bela-hand-comic` 两处目录，部分 Agent 共用后者；两处目录中的 11 个核心文件逐一与公开版比较 SHA-256，均一致。

这证明安装器可以部署所需文件，不代表六个客户端都已启动并成功识别、触发 Skill 或生成图片。本次没有安装六个客户端，也没有改动已有的全局 Skill。
