# 当前验证范围

核验日期：2026-09-21。首次发布使用已有的 7 张参考图；后续按用户要求新增白底示例、流程图与对比表，见文末记录。

| 检查项 | 状态 |
|---|---|
| 公开版文件、角色图与相对链接 | 已通过 |
| Skill 格式 | 已通过 |
| 七图拼版与已有输出保护 | 已通过：1536×2048 输出；重复输出返回错误且原文件未变 |
| 从公开仓库安装并核对文件 | 已通过：六种 Agent 目标的项目级文件安装，实际目录中核心文件一致 |
| GitHub README 图片显示与截图 | 已通过：默认效果、安装、自定义与环境三处实际截图 |
| 新图生成 | 已在 Codex 中传入贝拉参考生成：白底示例、流程图、对比表；不代表跨客户端完整实测 |
| Windows / Linux 拼版 | 未测试 |
| 六种 Agent 的逐个客户端触发与完整生图 | 未测试；安装器支持不等于出图能力可用 |
| 任意自定义角色、画风、排版的生成质量 | 由实际模型和任务决定，需逐张验收 |

测试通过只说明对应步骤可用，不保证中文、角色或概念关系无需人工检查。

## 本次实际检查

环境：macOS、Node.js 24.14.1、Skills CLI 1.7.0、FFmpeg 8.1.1。Skill 格式检查通过，文档相对链接存在，7 张原图均已打开检查。

在独立临时项目中，从公开仓库执行以下命令，未覆盖作者本机已安装的 Skill：

```sh
npx skills add belalee-ai/bela-hand-comic --skill bela-hand-comic --agent codex --copy --yes
```

安装器识别到 1 个 Skill。对 SKILL.md、风格指南、客户端展示配置、拼版脚本及 7 张图片，共 11 个核心文件逐一比较 SHA-256，均一致；安装后的 Skill 格式检查通过。这轮仅验证了 Codex 项目级安装。六种目标的后续安装检查见下一节；全局安装位置未实测。

拼版使用随包 7 张不同尺寸的参考素材作输入，仅用于检查脚本，不把它当成一套连续故事。输出为 1536×2048，已目视检查排列、留边与原图完整性；再次写入同一路径返回错误，原文件哈希未变。

README 在浏览器实际打开，参考图正常显示；前三张截图记录提交 b598352 的文档内容，新增 Agent 兼容截图记录提交 f897f62。没有把截图标成图片生成过程，也没有执行跨客户端新图生成测试。

## Agent 安装补充检查

2026-09-21，针对公开仓库提交 `d685928`，在另一个独立临时项目中执行：

```sh
npx skills add belalee-ai/bela-hand-comic --skill bela-hand-comic \
  --agent claude-code codex cursor opencode gemini-cli github-copilot \
  --copy --yes
```

安装器接受全部六个目标并正常完成。实际产生 `.claude/skills/bela-hand-comic` 和 `.agents/skills/bela-hand-comic` 两处目录，部分 Agent 共用后者；两处目录中的 11 个核心文件逐一与公开版比较 SHA-256，均一致。

这证明安装器可以部署所需文件，不代表六个客户端都已启动并成功识别、触发 Skill 或生成图片。该轮没有安装六个客户端，也没有改动已有的全局 Skill。

## 角色设计说明补充

2026-09-21，新增角色设计资料清单、模板和执行分支。Skill 格式与文档链接检查通过；角色设计指南已在 GitHub 浏览器实际打开检查，并保存页面截图。此项为说明与流程检查，没有新生成角色，不能据此宣称已完成自定义角色生图实测。

## 图示分支与贝拉 Demo

2026-09-21，公开版与作者本机 Skill 增加流程图、对比表、结构图、时间线及精确数据图表的分支。本机原文件已备份，原有默认人物设定保留。

按更新后的流程核对节点和比较维度后，在 Codex 内置 image_gen 中传入贝拉角色参考，生成两张新 Demo：

- `demo-flowchart.png`：白底墨线；检查了所有中文标签、满意分支和返回“定设定”的反馈回路。
- `demo-comparison.png`：粗蜡笔；检查了三列与两行的逐项对应、文字与阅读顺序；不含统计数据或评分。
- `demo-sample-first.png`：此前用户已确认的白底风格示例，复用加入画廊。

以上图片均为 1672×941 PNG，逐张目视检查；贝拉身份参考被用于新场景，Demo 不替代角色设定原图。新图提示词已随仓库保留，Skill 格式和受影响的本地链接已检查。这里记录的是在 Codex 中读取 Skill 后调用图片工具的结果，并非六种客户端的自动触发测试。真实数据绘图规则已补充，但本次两张 Demo 为定性图，没有实测精确数值绘图分支。

公开页面回读：提交 `8e2eca5` 已推送，GitHub README 中两张新图加载完整（原图均为 1672×941），已保存并目视检查 [新版页面截图](docs/images/github-diagram-demos.png)。

## 2026-09-21 简约知识卡片规则更新

本次为文档更新：新增可选简约版，检查入口链接、风格冲突、角色配饰规则和 Skill 格式。规则来自作者现有卡片迭代；本次未新增展示图，未重新测试各客户端安装与出图。既有故事场景参考图保持原用途。

## 三种类型扩展

2026-09-21，沿用现有 Skill 分支，在 Codex 内置 image_gen 中使用贝拉参考新增三张示例；本轮只扩充图库与说明，不改安装方式和 Skill 入口。

- 剪纸拼贴结构图：检查八处中文、三组父子关系与无箭头的组成连线；发现错腕和重复手环后局部修改，最终改为左手持剪刀、右手持纸片，青柠手环只保留在右腕。
- 黑白漫画时间线：检查三个阶段与动作对应，保留顺序示意说明；服装有意转为灰阶，手环保留青柠点色，不作为真实耗时数据。
- 水彩手账场景图：检查标题、人物和画本内容；画本中的柳树、桥与风景对应。一次网络错误未返回图片，重试后成功。

三张最终图片已目视检查。结构图与时间线均为 1672×941，水彩场景为 1448×1086。完整生成与修正提示词见 [本轮提示词](docs/more-style-prompts.md)。仍未进行跨 Agent 客户端的完整生成实测。

三种类型的公开页面已回读：提交 `d0f06f3` 的 README 中三张新图均加载完整，原图尺寸与本地一致；已检查并保存 [实际页面截图](docs/images/github-more-styles.png)。

## 简约知识卡与图库统一整理

2026-09-21，以已合入的 `fc44a9e` 简约版规则和现有风格图库为基础整合首页、图库与使用说明。区分表达方式、内容结构和笔触材质，保留故事、蜡笔、拼贴等原有示例，不将它们全部标成简约版。

新增展示图 `demo-simple-knowledge-card.png` 直接复用作者 JEV 系列已完成的“Jev 放在应用的哪一层”淡背景版，1448×1086 PNG，复制后 SHA-256 与原图一致。本次未重新生图。已检查层级、箭头、文字和贝拉右腕手环；卡片中的接法明确标为一种应用结构示意，并为 Jev 判断接口补充官方来源。

本机与公开版 `references/simple-knowledge-card.md` 内容一致；本次在公开版 SKILL 入口补上统一选图顺序与图库链接，未覆盖本机个人配置。Skill 格式与 Markdown 本地资源链接检查通过；安装和跨客户端出图测试沿用此前明确范围。
